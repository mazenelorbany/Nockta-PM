// TOMBSTONE — DELETE THIS FILE.
//
// This was editor cruft: a duplicate of StandupPage.tsx that crept in via a
// "Save As" / autosave race. The duplicate is removed; this shim re-exports
// from the real page so anything that accidentally imports the bad path still
// works until the file is removed from the repo (the agent that wrote this
// shim doesn't have shell access to `rm` it).
//
// Action: run `rm "apps/web/src/pages/StandupPage 2.tsx"` and commit.

export { StandupPage } from './StandupPage';
