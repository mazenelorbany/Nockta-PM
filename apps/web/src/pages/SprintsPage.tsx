// =============================================================================
// SprintsPage was merged into ProjectBacklogPage so the backlog and sprint
// management live on one page. This file is intentionally empty — kept as a
// stub so any straggling import that escaped grep gets a clean "no export"
// TypeScript error rather than a 404 at runtime.
//
// The /projects/:projectId/sprints route now redirects to /backlog via the
// RedirectToBacklog wrapper in App.tsx.
// =============================================================================

export {};
